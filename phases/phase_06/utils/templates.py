"""Phase 6 — zero-Gemini template responses."""

TEMPLATES: dict[str, str] = {
    "cart_added": "added {item} from {restaurant} ✓",
    "cart_cleared": "cart cleared, starting fresh 🔄",
    "swiggy_down": "swiggy's taking a nap — try again in a sec?",
}
